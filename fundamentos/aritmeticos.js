const [a, b, c, d] = [3, 5, 1, 15] //forma reduzida de criar 4 variáveis usando operador de structuring
//são operadores binários porque operam com dois operandos
const soma = a + b + c + d
const sub = d - b
const mult = a * b
const div = d / a
const modulo = a % 2

console.log(soma, sub, mult, div, modulo)
